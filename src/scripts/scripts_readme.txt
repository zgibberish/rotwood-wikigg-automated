These are shell scripts used to automate extracting specific image assets,
like powers or gems icons.
You are expected to be in the same directory as a Rotwood data.zip archive.
You will need rwtextool for most of these, use setup_rwtextool.sh to install
required packages and set up a Python virtual environment (you will only need to
do this once).
